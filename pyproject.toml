[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"

[project]
name = "nexrag"
version = "0.4.0"
description = "Framework-agnostic RAG pipeline SDK. Plug in any component, swap any stage, configure everything in YAML"
readme = "README.md"
license = { text = "Apache-2.0" }
authors = [
    { name = "KevinRawal", email = "kevinrawal30@gmail.com" }
]
requires-python = ">=3.12"
keywords = [
    "rag",
    "retrieval-augmented-generation",
    "llm",
    "vector-database",
    "embeddings",
    "ai",
    "nlp",
    "pipeline",
    "sdk",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]

# Keep this minimal. Only what the pipeline skeleton needs. Providers (chromadb,
# openai, anthropic, ...) are optional extras — every adapter imports its SDK
# lazily, so `pip install nexrag` stays light and you add only what you use.
dependencies = [
    "pydantic>=2.0",   # config schema validation
    "pyyaml>=6.0",     # YAML config loading
]

[project.optional-dependencies]

# Provider extras — install only what you use.
# These floors are the single source of truth; nothing pins them in core.
openai = [
    "openai>=2.38.0",
]
anthropic = [
    "anthropic>=0.20",
]
gemini = [
    "google-genai>=1.0",
]
ollama = [
    "ollama>=0.1",
    "httpx>=0.27",
]
chromadb = [
    "chromadb>=1.5.9",
]
pinecone = [
    "pinecone>=5.0",
]
huggingface = [
    "huggingface-hub>=0.20",
]

# Chunking strategy extras
tiktoken = [
    "tiktoken>=0.7",
]
code = [
    "tree-sitter>=0.21",
    "tree-sitter-language-pack>=0.2",
]

# Guardrail extras
pii = [
    "presidio-analyzer>=2.2",
    "presidio-anonymizer>=2.2",
]

# Document loaders
pdf = [
    "pypdf>=4.0",
]
word = [
    "python-docx>=1.0",
]
excel = [
    "openpyxl>=3.1",
]
html = [
    "beautifulsoup4>=4.12",
    "lxml>=5.0",
]
bm25 = [
    "rank-bm25>=0.2",
]
cohere = [
    "cohere>=5.0",
]
cross-encoder = [
    "sentence-transformers>=2.7",
]

# Convenience bundles
all-providers = [
    "nexrag[openai]",
    "nexrag[anthropic]",
    "nexrag[gemini]",
    "nexrag[ollama]",
    "nexrag[chromadb]",
    "nexrag[pinecone]",
    "nexrag[huggingface]",
]
all-chunkers = [
    "nexrag[tiktoken]",
    "nexrag[code]",
]
all-guards = [
    "nexrag[pii]",
]
all-loaders = [
    "nexrag[pdf]",
    "nexrag[word]",
    "nexrag[excel]",
    "nexrag[html]",
]
all-sparse = [
    "nexrag[bm25]",
]
all-rerankers = [
    "nexrag[cohere]",
    "nexrag[cross-encoder]",
]
all-retrieval = [
    "nexrag[all-sparse]",
    "nexrag[all-rerankers]",
]
# Full bundle — dev/CI only; installs heavy deps (PyTorch via sentence-transformers)
all = [
    "nexrag[all-providers]",
    "nexrag[all-loaders]",
    "nexrag[all-retrieval]",
    "nexrag[all-chunkers]",
    "nexrag[all-guards]",
]

# Dev dependencies
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.4",        # linter + formatter
    "mypy>=1.10",       # type checker
    "pre-commit>=3.7",
    "types-pyyaml",
    "rank-bm25>=0.2",   # BM25Retriever tests run the real algorithm, not a mock
]

[project.urls]
Homepage = "https://github.com/kevinrawal/nexrag"
Repository = "https://github.com/kevinrawal/nexrag"
Issues = "https://github.com/kevinrawal/nexrag/issues"
Changelog = "https://github.com/kevinrawal/nexrag/blob/main/CHANGELOG.md"

# Ruff (linter + formatter)

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
]
ignore = ["E501"]  # line length handled by formatter

[tool.ruff.lint.isort]
known-first-party = ["nexrag"]

# Mypy

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true
mypy_path = "src"

# Optional provider/loader deps — imported lazily inside adapters and installed
# only via extras. CI type-checks the base package without extras, so suppress
# missing-import errors regardless of install state. (ignore_missing_imports is
# safe whether or not the package is present, unlike an inline `# type: ignore`.)
[[tool.mypy.overrides]]
module = [
    "rank_bm25",
    "chromadb",
    "openai",
    "anthropic",
    "google.genai",
    "google.genai.*",
    "ollama",
    "httpx",
    "huggingface_hub",
    "cohere",
    "sentence_transformers",
    "pypdf",
    "pinecone",
    "tiktoken",
    "tree_sitter",
    "tree_sitter_language_pack",
    "presidio_analyzer",
    "presidio_anonymizer",
]
ignore_missing_imports = true

# Pytest

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
markers = [
    "integration: marks tests that require live services (deselect with '-m not integration')",
    "unit: marks fast, isolated unit tests",
]

[tool.coverage.run]
source = ["src/nexrag"]
omit = ["*/tests/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
]
